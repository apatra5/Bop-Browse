# Bop-Browse Backend Environment Variables

# Database Configuration (REQUIRED)
# PostgreSQL connection string with pgvector extension
DATABASE_URL=postgresql://username:password@localhost:5432/bopbrowse

# For Railway:
# DATABASE_URL will be automatically set by Railway when you add a PostgreSQL service

# For Render:
# Use the "Internal Database URL" from your PostgreSQL service

# For local development:
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/bopbrowse

# Note: Make sure to enable pgvector extension:
# psql $DATABASE_URL -c "CREATE EXTENSION IF NOT EXISTS vector;"
